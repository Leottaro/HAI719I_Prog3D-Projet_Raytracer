{
	"version": "2.0.0",
    "tasks": [
        {
            "label": "clean-and-build",
            "type": "shell",
            "command": "make clean && make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
	]
}